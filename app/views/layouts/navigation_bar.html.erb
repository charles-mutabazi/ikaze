<nav class="navbar navbar-fixed-top main-navbar">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Ikaze Iwacu", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><a href="#"> <%= icon('info-circle')%> About</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if user_signed_in? %>
  	      	<%= link_to new_post_path, data: { modal: true }, :class => "link_btn" do %>
  	      		<button><%= icon('pencil')%></i> Your Impression</button>
  	      	<% end %>
        	<% end %>
        </li>
        <li>
          <%= link_to root_path do %>
            <%= icon('home')%> Home
          <% end %>
        </li>
        <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= icon('user')%>
            <% if current_user.name? %>
              <%= current_user.name.partition(' ').first.titleize %><span class="caret"></span></a>
            <% else %>
              <%= current_user.email.partition('@').first %> <!-- Elegant Code -->
              <span class="caret"></span></a>
            <% end %>

          <ul class="dropdown-menu">
            <li>
              <%= link_to current_user do %>
              <%= image_tag current_user.avatar.url(:sm_thumb), class: "img-circle" %> &nbsp;&nbsp;Profile
              <% end %>
            </li>
            <li class="disabled"><a href="#"> <%= icon('exclamation-circle') %> Notifications</a></li>
            <li role="separator" class="divider"></li>
            <li class="dropdown-header">Advanced</li>
            <li>
              <%= link_to edit_user_registration_path do %>
    						<%= icon('cog')%> Account Settings
    					<% end %>
            </li>
            <li>
              <%= link_to destroy_user_session_path, :method => :delete do %>
  						<%= icon('sign-out')%> Sign Out
              <% end %>
          </li>
          </ul>
        </li>
    		<% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
